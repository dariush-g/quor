# Compilation Flags
change target architecture: --target-arch=$target_arch
change target operating system: --target-os=$target_os
show debug print statements: --debug

emit tokens: --emit-tokens
emit ast: --emit-ast
emit typed: --emit-typed
emit mir: --emit-mir
emit assembly: --emit-asm
