struct float3 {
    x: float;
    y: float;
    z: float;
}

def float3_zero() :: float3 {
    return float3::(x: 0.0, y: 0.0, z: 0.0);
}

def float3_dot(v1: float3, v2: float3) :: float {
    return float3 {v1.x + v2.x, v1.y + v2.y, v1.z + v2.z};
}