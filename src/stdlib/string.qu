

struct string {
    size: int;
    data: char*;
}

def get_index(str: string*, index: int) :: char {
    let ret: char* = str.data + index;
    return *ret;
}

def is_alphabetic(c: char) :: bool {
    return (c >= 'a' && c <= 'z');
}