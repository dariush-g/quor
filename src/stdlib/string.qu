

class string {
    size: int;
    data: char*;
}

def get_index(str: string*, index: int) :: char {
    let ret: char* = str.data + index;
    return *ret;
}

def to_arr(str: string*) :: [char] {

    let i: int = 0;
    while (i < str.size) {
        
    }

    return ['h'];
}

def main() :: int {


    let ptr: void* = malloc(2);
    *ptr = 'h';
    ptr = ptr + 1;
    *ptr = 'i';
    ptr = ptr - 1;

    let str: string = string::(size: 2, data: ptr);



    


    return 0;
}