#

```quor

def add(a: int, b: int) -> int {
    return a + b;
} 

def main() :: int {
    var n: int = add(1, 2);

    print_int(n);


    let ch: char = 'h';

    let num: int = ch~int;

    return 0;
}

```
