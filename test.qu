fn example()->void {
    var value: int = 5;
    var x: int* = &value;
    *x = 100;
}

fn main() -> int {
    var x: bool = true;
    if (x) {
        var n: int = 0;
    }    

    return 0;
}


// nasm -f elf64 test.asm -o test.o && ld test.o -o test && ./test
