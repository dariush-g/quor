@import::<mem>;
@import::<io>;

def main() :: int {

    let ptr: void* = malloc(2);

    *ptr = 'h';

    ptr = ptr + 1;

    *ptr = 'i';

    ptr = ptr - 1;

    print_char(*(ptr + 1));
    return 0;
}
