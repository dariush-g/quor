@import::<string.qu>;



def main() :: int {

    let ptr: void* = malloc(2);
    
    *ptr = 'h';
    ptr = ptr + 1;
    *ptr = 'i';
    ptr = ptr - 1;


    let str1: string* = string::(size: 2, data: ptr); 
    
    let str: string* = "hello";

    
    print_char(get_index(str, 2));

    return 0;
}
